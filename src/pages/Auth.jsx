import React from 'react';
import Authentication from '../components/Authentication/Authentication';

const Auth = () => {
    return (
        <div>
            <Authentication />
        </div>
    );
};

export default Auth;